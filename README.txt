1. define variables:
    let: once define, local variables
    var: multiply define, global variables

2. define constant:
    const PI = 3.14
    *immutable
    *have to initialize with value

3. deconstruction:
    let [x,y,z] = [1, 2, 3]

4. template string:
    use ` and ${}
    can do alogrithm and line break

5. declare objects:
    const user2 = {name,age}

6. assign objects:
    let someone1 = { ...person1} //copy objects

7. arrow fuctions:
    var f1 = function(a) {
        return a
    }

    var f2 = a => a //anoymous fuctions